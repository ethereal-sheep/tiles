[package]
name = "tiles"
version = "0.1.0"
edition = "2021"

[dependencies]
wgpu = "22"
winit = "0.30"
bytemuck = { version = "1", features = ["derive"] }
pollster = "0.4"
glam = "0.29"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
